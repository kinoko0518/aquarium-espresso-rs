[package]
name = "aquarium-espresso-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
rayon = "1.10"
macroquad = "0.4.14"
image = { version = "0.25", default-features = false, features = ["jpeg", "png"] }
rand = "0.8"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
